def finddiffpair(a,n):
    x=0;y=1
    while y <len(a) and x<len(a):
        diff=a[y]-a[x]
        if diff <n:
            y=y+1
        elif diff>n:
            x=x+1
        if diff==n:
            return a[y],a[x]
a=[5, 20, 3, 2, 50, 80]
n=78
list.sort(a)
print finddiffpair(a,n)